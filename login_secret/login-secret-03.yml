properties:
  uaa:
    clients:
      login:
        id: login
        override: true
        secret: login_override_secret
        authorized-grant-types: authorization_code,client_credentials,refresh_token
        authorities: oauth.login
        scope: openid,oauth.approvals
        redirect-uri: https://#{protocol}.<%= properties.domain %>